---
interface Props {
  story: number;
}

const { story } = Astro.props;

const storyLabels: Record<number, string> = {
  1: 'AI Systems',
  2: 'Client Work',
  3: 'Solo Founder + AI',
};

const label = storyLabels[story] ?? 'Unknown';
---

<span class={`story-badge story-${story}`}>{label}</span>
